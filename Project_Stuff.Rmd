---
title: "Project Stuff"
author: "Owen Wright"
date: "11/15/2018"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
```

```{r}
Elliot_run<-read_csv("Elliot/activities.csv")%>% #Elliot's data messing
  filter(type=="Run")%>%
  mutate(miles=distance/1609,
         minutes=elapsed_time/60,
         pace=minutes/miles)%>%
  select(-type)
Justin_run<-read_csv("Fursnatch/activities.csv")%>% #Justin's data messing
  filter(type=="Run")%>%
  mutate(miles=distance/1609,
         minutes=elapsed_time/60,
         pace=minutes/miles)%>%
  select(-type)
Mowgli_act<-activities
Mowgli_run<-read_csv("Mowgli/activities.csv")%>% 
  filter(type=="Run")%>%
  mutate(miles=distance/1609,
         minutes=elapsed_time/60,
         pace=minutes/miles)%>%
  select(-type)
Birdo_run<-read_csv("Birdo/activities.csv")%>%
  filter(type=="Run")%>%
  mutate(miles=distance/1609,
         minutes=elapsed_time/60,
         pace=minutes/miles)%>%
  select(-type)
Jackson <- read_csv("~/Mscs 264 F18/Submit Section B/BigO-Strava/Jackson/activities.csv")
Jackson_run<-Jackson%>%
  filter(type=="Run")%>%
  mutate(miles=distance/1609,
         minutes=elapsed_time/60,
         pace=minutes/miles)%>%
  select(-type,-gear)
Finn_run<-read_csv("Finn/Finn/activities.csv")%>%
  filter(type=="Run")%>%
  mutate(miles=distance/1609,
         minutes=elapsed_time/60,
         pace=minutes/miles)%>%
  select(-type,-gear)
Plumb_run<-read_csv("Plumb/activities.csv")%>%
  filter(type=="Run")%>%
  mutate(miles=distance/1609,
         minutes=elapsed_time/60,
         pace=minutes/miles)%>%
  select(-type,-gear)
```
